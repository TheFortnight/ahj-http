/*const cr = require('./js/api/createRequest');
const btn = document.querySelector('.test');
btn.addEventListener('click', async (e)=>{
    e.preventDefault();
    const options = {
        method: 'GET',
        url: '',
        body: {method: 'allTickets'}
    }
    const resp = await cr(options);
    alert('here it goes: '+resp)
});*/
import './js/app';
import './style.css';